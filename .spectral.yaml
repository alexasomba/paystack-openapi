# Spectral ruleset for this repo.
# Vacuum uses `ruleset-recommended.yaml` via `vacuum.conf.yaml`; Spectral uses its own ruleset format.
extends:
  - spectral:oas

rules:
  # This component is used for SDK webhook type generation
  # referenced index internal paths below
  oas3-unused-component: error
