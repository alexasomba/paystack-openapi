type: object
properties:
  status:
    type: boolean
  message:
    type: string
  data:
    type: object
    properties:
      integration:
        type: integer
      domain:
        type: string
      name:
        type: string
      description:
        type: string
        nullable: true
      amount:
        type: integer
        nullable: true
      currency:
        type: string
      slug:
        type: string
      active:
        type: boolean
      id:
        type: integer
      createdAt:
        type: string
      updatedAt:
        type: string
      products:
        type: array
        items:
          $ref: ./PageProductsArray.yaml
    required:
      - integration
      - domain
      - name
      - amount
      - currency
      - slug
      - active
      - id
      - createdAt
      - updatedAt
      - products
required:
  - status
  - message
  - data
