type: object
properties:
  status:
    type: boolean
  message:
    type: string
  data:
    type: object
    properties:
      name:
        type: string
      description:
        type: string
        nullable: true
      amount:
        type: integer
        nullable: true
      split_code:
        type: string
        nullable: true
      integration:
        type: integer
      domain:
        type: string
      slug:
        type: string
      currency:
        type: string
      type:
        type: string
      collect_phone:
        type: boolean
      active:
        type: boolean
      published:
        type: boolean
      migrate:
        type: boolean
      id:
        type: integer
      createdAt:
        type: string
      updatedAt:
        type: string
    required:
      - name
      - amount
      - integration
      - domain
      - slug
      - currency
      - type
      - collect_phone
      - active
      - published
      - migrate
      - id
      - createdAt
      - updatedAt
required:
  - status
  - message
  - data
