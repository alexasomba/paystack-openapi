post:
  tags:
    - Card Preauthorization
  summary: Reserve Preauthorization
  description: Hold an amount using an existing customer's authorization that's marked reusable.
  operationId: preauthorization_reserve_authorization
  requestBody:
    content:
      application/json:
        schema:
          $ref: ../components/schemas/PreAuthorizationReserve.yaml
      application/x-www-form-urlencoded:
        schema:
          $ref: ../components/schemas/PreAuthorizationReserve.yaml
  responses:
    "200":
      description: ""
      content:
        application/json:
          schema:
            $ref: ../components/schemas/PreAuthorizationReserveWithAuthCodeResponse.yaml
    "401":
      $ref: ../components/responses/Unauthorized.yaml
    default:
      description: Server error
